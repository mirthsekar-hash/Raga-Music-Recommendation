# Lib

Server and shared business logic. See [docs/folderStructure.md](../docs/folderStructure.md) for the full layout.

**Server-only modules** (must use `import "server-only"`):
- `lib/gemini/`
- `lib/supabase/admin.ts`
- `lib/env.ts`
