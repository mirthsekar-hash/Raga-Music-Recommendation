# Supabase

Database migrations and local Supabase config.

- `migrations/0001_init.sql` — schema for songs, artists, sessions, user_taste_profile, feedback + RLS policies

Apply via Supabase CLI or the Supabase dashboard SQL editor.
