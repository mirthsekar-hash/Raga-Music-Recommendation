# Data

Static seed files for Phase 1. Not imported at runtime by the Next.js app.

- `artists.seed.json` — sample artist records
- `songs.seed.json` — sample song records (~200 tracks)

Populated via `npx tsx scripts/seed.ts`.
